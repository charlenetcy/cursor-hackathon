{
  "name": "cursor-hackathon",
  "version": "1.0.0",
  "description": "Prompt-Driven Multiplayer Parkour World",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:style\"",
    "dev:server": "cd apps/server && npm run dev",
    "dev:client": "cd apps/client && npm run dev",
    "dev:style": "cd services/prompt && npm run dev",
    "build": "npm run build:shared && npm run build:server && npm run build:client && npm run build:style",
    "build:shared": "cd shared && npm run build",
    "build:server": "cd apps/server && npm run build",
    "build:client": "cd apps/client && npm run build",
    "build:style": "cd services/prompt && npm run build",
    "install:all": "npm install && cd apps/server && npm install && cd ../../apps/client && npm install && cd ../../services/prompt && npm install && cd ../../shared && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.0"
  }
}
